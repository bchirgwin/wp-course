"use strict";

// create a function countTo (num) that will prints all numbers up to the parameter.

function countTo(num) {
  for (let i = 0; i <= num; i++) {
    console.log(i);
  }
}
countTo(3);
countTo(5);
countTo(10);
countTo(15);
