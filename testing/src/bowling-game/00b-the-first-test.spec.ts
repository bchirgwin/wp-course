import { describe, it } from '@jest/globals';
import { BowlingGame } from './00b-bowling-game';

describe('BowlingGame', () => {
  it('handle gutter game', () => {
    const game = new BowlingGame();
  });
});
