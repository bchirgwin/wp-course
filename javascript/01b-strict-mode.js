"use strict";

// use strict enabled by added "use strict"; as the first line of the file

// strict mode
// - variables must be declared with var, let or const

let x = 42; // change to var x = 42
console.log(x);
